extends library

block div
  .library
    each pictogramma in pictograms
      img.picta(scr= pictogramma.img)
  footer
    a(href='/library')  
      img.exit(scr='/app_server/views/symbols/cross.png')
